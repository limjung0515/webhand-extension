/**
 * 공유 설정 타입
 */
export interface ShareSettings {
    googleSheetsUrl?: string;    // Google Apps Script 웹앱 URL
    emailAddress?: string;        // 받는 사람 이메일 주소
    kakaoAppKey?: string;         // 카카오 JavaScript SDK 앱 키
}

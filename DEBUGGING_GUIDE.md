# 🔧 메시징 디버깅 가이드

## 📝 업데이트 내용 (방금 빌드됨!)

Side Panel에 상세한 로그를 추가했습니다. 이제 버튼을 클릭하면:

1. 콘솔에 자세한 메시지 표시
2. 오류 발생 시 alert 팝업
3. 각 단계별 상태 추적

---

## 🧪 테스트 방법

### 1. Extension 새로고침
```
1. chrome://extensions 접속
2. WebHand 확장프로그램 찾기
3. 새로고침 버튼 (🔄) 클릭
```

### 2. 웹사이트 접속
```
https://naver.com
```

### 3. Side Panel 열기
- 우측 하단 "📊 WebHand" 버튼 클릭
- 또는 확장프로그램 아이콘 클릭

### 4. 콘솔 열기 (중요!)

**Side Panel 콘솔:**
1. Side Panel에서 우클릭 → **"검사"**
2. Console 탭 확인

**Content Script 콘솔:**
1. 웹 페이지에서 F12 또는 우클릭 → **"검사"**
2. Console 탭 확인

**Background Worker 콘솔:**
1. `chrome://extensions` 접속
2. WebHand 카드에서 **"Service Worker"** 클릭
3. Console 확인

### 5. 버튼 클릭 테스트

#### "📖 페이지 읽기" 클릭 시 예상 로그:

**Side Panel Console:**
```
🔵 handleReadPage called
🔵 Current tab: {id: 123, url: "https://naver.com", ...}
🔵 Sending READ_PAGE message to tab: 123
✅ Page content received: {...}
```

**Content Script Console (페이지):**
```
🌐 WebHand Content Script loaded on: https://naver.com
📨 Message received in content script: READ_PAGE
📖 Reading page content...
```

**만약 오류가 난다면:**
- alert 팝업으로 오류 메시지 표시
- Console에 빨간색 에러 출력

---

## 🐛 예상 문제 & 해결법

### 문제 1: "Could not establish connection"
**원인:** Content Script가 로드되지 않음

**해결:**
1. 페이지 새로고침 (F5)
2. chrome://extensions에서 Extension 새로고침
3. 페이지 다시 새로고침

### 문제 2: Tab ID가 undefined
**원인:** Side Panel이 활성 탭을 찾지 못함

**해결:**
1. 웹사이트 탭이 활성화되어 있는지 확인
2. Side Panel을 닫고 다시 열기

### 문제 3: 메시지가 전송되지만 응답 없음
**원인:** Content Script의 메시지 리스너 문제

**해결:**
1. Content Script 콘솔 확인
2. 에러 메시지 확인
3. 네트워크 탭에서 스크립트 로드 확인

---

## 📊 디버깅 체크리스트

### ✅ 빌드 & 로드
- [ ] `npm run build` 실행됨
- [ ] Chrome에서 확장프로그램 새로고침
- [ ] 웹 페이지 새로고침

### ✅ 콘솔 확인
- [ ] Side Panel 콘솔 열림
- [ ] 웹 페이지 콘솔 열림 (Content Script)
- [ ] Background Worker 콘솔 열림

### ✅ 버튼 클릭
- [ ] "📖 페이지 읽기" 클릭
- [ ] 로딩 상태 변경 확인
- [ ] 콘솔 로그 확인
- [ ] 결과 또는 에러 확인

---

## 🔍 다음 단계

### 메시지가 정상 작동하면:
1. ✅ 결과 데이터 확인
2. ✅ UI 개선 (Table로 표시)
3. ✅ CSV 다운로드 추가

### 메시지가 작동하지 않으면:
1. 🔧 콘솔 로그 공유
2. 🔧 정확한 에러 메시지 확인
3. 🔧 권한 문제 확인

---

## 💬 로그 예시

### 성공적인 경우:
```bash
# Side Panel Console
🔵 handleReadPage called
🔵 Current tab: {id: 123, url: "https://naver.com", title: "NAVER"}
🔵 Sending READ_PAGE message to tab: 123
✅ Page content received: {
  success: true,
  content: {
    url: "https://naver.com",
    title: "NAVER",
    text: "네이버 메인에서...",
    links: [...]
  }
}
```

### 오류가 발생한 경우:
```bash
# Side Panel Console
🔵 handleReadPage called
🔵 Current tab: {id: 123, url: "https://naver.com"}
🔵 Sending READ_PAGE message to tab: 123
❌ Failed to read page: Error: Could not establish connection. Receiving end does not exist.
```

이런 오류가 나면 → Content Script가 로드되지 않은 것!

---

## 🚀 테스트 시작!

1. 위 가이드대로 콘솔 3개 열기
2. 버튼 클릭
3. 로그 확인
4. 결과 보고!

**로그를 보내주시면 정확히 문제를 파악하고 수정하겠습니다!** 🎯
